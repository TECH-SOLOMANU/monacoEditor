# Extension-Based Uploads

Run `node server.js` from the project root, then open `http://localhost:3000/docs/liveTest/`.

- Uploaded `.js` files are saved in `jsFiles/`.
- Uploaded `.html` files are saved in `htmlFiles/`.

GitHub Pages can display the demo but cannot save uploads because it does not run the local server.
